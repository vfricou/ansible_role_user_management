---
- name: 'import validator tasks'
  ansible.builtin.import_tasks: validator/check_os.yml

- name: 'INCLUDE => user management tasks'
  ansible.builtin.include_tasks: processing/main.yml
  loop: "{{ user_list | dict2items }}"
